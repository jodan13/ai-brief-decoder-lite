services:
  db:
    image: postgres:16-alpine
    container_name: ai-brief-decoder-lite-db
    environment:
      POSTGRES_DB: brief_decoder
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d brief_decoder"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
